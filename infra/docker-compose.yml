# Паша, привет!
# Наставники нам совсем перестали отвечать, поэтому я решила пока
# на этом этапе отправить на ревью. Чтобы сразу что-нибудь подправить.
# Не могу разобраться, почему при попытке регистрации выдает 500,
# соответственно пока не наполняю сайт ((
# Надеюсь на наставление на путь истинный или хотя бы подзатыльник
version: '3'
services:

  db:
    image: postgres:13.10
    volumes:
      - pg_data:/var/lib/postgresql/data/
    env_file:
      - ./.env

  backend:
    image: v1tox/foodgram_backend:latest
    volumes:
      - static_value:/app/static
      - media_value:/app/media/
    depends_on:
      - db
    env_file:
      - ./.env

  frontend:
    image: v1tox/foodgram_frontend:latest
    volumes:
      - ../frontend/:/app/result_build/
    depends_on:
      - backend

  nginx:
    image: nginx:1.19.3
    ports:
      - 8080:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ../frontend/build:/usr/share/nginx/html/
      - ../docs/redoc.html:/usr/share/nginx/html/api/docs/redoc.html
      - static_value:/var/html/static/
      - media_value:/var/html/media/
    depends_on:
      - frontend

volumes:
  pg_data:
  static_value:
  media_value:
